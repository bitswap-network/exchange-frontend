const Link = {
    variants: {
        default: {
            _hover: {
                textDecoration: "none",
            },
        },
    },
    defaultProps: {
        variant: "default",
    },
};

export default Link;
